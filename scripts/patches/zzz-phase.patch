diff --git a/Resources/pet/skins/cat/cat-pet.js b/Resources/pet/skins/cat/cat-pet.js
index fd36e4c..0017fff 100644
--- a/Resources/pet/skins/cat/cat-pet.js
+++ b/Resources/pet/skins/cat/cat-pet.js
@@ -109,16 +109,25 @@ vec4 over(vec4 a,vec4 b){
  float alpha=a.a+b.a*(1.0-a.a);
  return vec4((a.rgb*a.a+b.rgb*b.a*(1.0-a.a))/max(alpha,.00001),alpha);
 }
-vec4 glyph(vec4 box,float phase){
+/* LOCAL FIX on top of the vendored kit (see scripts/patches/zzz-phase.patch).
+   Upstream shipped phases 0 / .33 / .66 for small / medium / large, which
+   retires them bottom, TOP, middle — no direction at all, and the eye reads
+   the adjacent pairs as the Zs dropping. Bottom, middle, top 1.2s apart is
+   what makes it one wave going up. 'side' had to come out of 'phase' first:
+   one value was carrying both the timing and which half of the shared
+   diagonal a glyph keeps, so the phases could not be swapped on their own.
+   Travel went .037 -> .100 of uv as well, because .037 is 3.9px on a 120px
+   canvas and four pixels in 3.6s is a blink, not a drift. */
+vec4 glyph(vec4 box,float phase,float side){
  float cycle=fract(zTime/3.6+phase);
- vec2 offset=vec2(.009*sin(cycle*6.283+phase*2.0),-.037*cycle)*zStrength;
+ vec2 offset=vec2(.009*sin(cycle*6.283+phase*2.0),-.100*cycle)*zStrength;
  vec2 q=uv-offset;
  vec4 c=texture2D(texture0,q);
- float fade=smoothstep(0.0,.18,cycle)*(1.0-smoothstep(.72,1.0,cycle));
+ float fade=smoothstep(0.0,.25,cycle)*(1.0-smoothstep(.72,1.0,cycle));
  // Overlapping bounding rectangles share a diagonal gap, not any ink.
  float separate=smoothstep(214.5/384.0,216.5/384.0,q.x-q.y);
- if(phase>.5)c.a*=separate;
- else if(phase>.1)c.a*=1.0-separate;
+ if(side>1.5)c.a*=separate;
+ else if(side>0.5)c.a*=1.0-separate;
  c.a*=inside(q,box)*mix(1.0,fade,min(zStrength,1.0));
  return c;
 }
@@ -130,9 +139,9 @@ void main(){
   vec4 medium=vec4(294.0,99.0,320.0,126.0)/384.0;
   vec4 large=vec4(313.0,64.0,356.0,108.0)/384.0;
   c.a*=1.0-max(inside(uv,small),max(inside(uv,medium),inside(uv,large)));
-  c=over(glyph(small,0.0),c);
-  c=over(glyph(medium,.33),c);
-  c=over(glyph(large,.66),c);
+  c=over(glyph(small,0.0,0.0),c);
+  c=over(glyph(medium,2.0/3.0,1.0),c);
+  c=over(glyph(large,1.0/3.0,2.0),c);
  }
  gl_FragColor=c;
 }`;
